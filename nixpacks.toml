[phases.install]
cmds = [
  "composer install --ignore-platform-reqs --no-interaction --prefer-dist --optimize-autoloader --no-dev --no-scripts",
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
